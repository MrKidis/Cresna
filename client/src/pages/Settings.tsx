import { UnpaidWorkspaceState, WorkspaceFrame } from "@/components/WorkspaceFrame";
import { useUnpaidPreview } from "@/contexts/UnpaidPreviewContext";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { PlayCircle, ShieldCheck, Store } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

export default function Settings() {
  const { isUnpaidPreview, isCheckingPreview } = useUnpaidPreview();
  const { user } = useAuth();
  const { data: profile } = trpc.profile.me.useQuery();
  const { data: onboarding } = trpc.onboarding.me.useQuery();
  const { data: intelligence } = trpc.intelligence.overview.useQuery();
  const [, setLocation] = useLocation();
  const [workspaceName, setWorkspaceName] = useState("");
  const utils = trpc.useUtils();
  const updateWorkspaceName = trpc.profile.updateWorkspaceName.useMutation({ onSuccess: () => { utils.profile.me.invalidate(); toast.success("Workspace name saved"); }, onError: () => toast.error("Could not save your workspace name") });
  const replayTutorial = trpc.onboarding.setStatus.useMutation({ onSuccess: () => { utils.onboarding.me.invalidate(); toast.success("Cresna’s tutorial will start now"); }, onError: () => toast.error("Could not restart the tutorial") });
  useEffect(() => { setWorkspaceName(profile?.workspaceName || ""); }, [profile?.workspaceName]);
  return <WorkspaceFrame eyebrow="Settings" title="Your Cresna workspace." description="Manage identity, workspace preferences, onboarding, and store connection controls. Payment credentials and Shopify tokens are never displayed here.">{isUnpaidPreview || isCheckingPreview ? <UnpaidWorkspaceState title="Settings are unavailable in the unpaid preview" detail="This preview intentionally contains no account, connection, or billing record." /> : <div className="grid max-w-4xl gap-5"><section className="rounded-[1.35rem] border border-border bg-card p-6 text-card-foreground sm:p-8"><p className="eyebrow text-[10px] text-muted-foreground">Profile</p><div className="mt-7 grid gap-6 border-t border-border pt-7 sm:grid-cols-2"><div><p className="text-[11px] font-bold text-muted-foreground">Name</p><p className="mt-2 text-sm font-semibold text-foreground">{user?.name || "Not provided"}</p></div><div><p className="text-[11px] font-bold text-muted-foreground">Email</p><p className="mt-2 text-sm font-semibold text-foreground">{user?.email || "Not provided"}</p></div></div><form className="mt-8 border-t border-border pt-7" onSubmit={event => { event.preventDefault(); updateWorkspaceName.mutate({ workspaceName }); }}><Label htmlFor="workspace-name" className="text-xs font-bold text-foreground">Workspace name</Label><div className="mt-3 flex flex-col gap-3 sm:flex-row"><Input id="workspace-name" value={workspaceName} maxLength={120} onChange={event => setWorkspaceName(event.target.value)} placeholder="e.g., Northstar Commerce" className="h-11 rounded-xl border-border bg-background" /><Button type="submit" disabled={updateWorkspaceName.isPending} className="h-11 rounded-full px-5 text-xs font-bold">{updateWorkspaceName.isPending ? "Saving…" : "Save name"}</Button></div><p className="mt-3 text-[11px] text-muted-foreground">This name labels your Cresna workspace. Your sign-in identity remains managed by your account provider.</p></form></section><section className="grid gap-5 md:grid-cols-2"><article className="rounded-[1.35rem] border border-border bg-card p-6 text-card-foreground"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-secondary text-secondary-foreground"><PlayCircle className="h-5 w-5" /></span><div><p className="eyebrow text-[10px] text-muted-foreground">Onboarding</p><h2 className="mt-2 text-lg font-extrabold tracking-[-0.04em]">Need a refresher?</h2></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">Tutorial status: <strong className="text-foreground">{onboarding?.status === "completed" ? "completed" : onboarding?.status === "dismissed" ? "skipped" : "not started"}</strong>. It never opens again automatically after you complete or skip it.</p><Button onClick={() => replayTutorial.mutate({ status: "not_started" })} disabled={replayTutorial.isPending} variant="outline" className="mt-5 rounded-full text-xs font-bold">{replayTutorial.isPending ? "Restarting…" : "Replay tutorial"}</Button></article><article className="rounded-[1.35rem] border border-border bg-card p-6 text-card-foreground"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-secondary text-secondary-foreground"><Store className="h-5 w-5" /></span><div><p className="eyebrow text-[10px] text-muted-foreground">Connected store</p><h2 className="mt-2 text-lg font-extrabold tracking-[-0.04em]">{intelligence?.store ? intelligence.store.myshopifyDomain : "No Shopify store connected"}</h2></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{intelligence?.store ? "Review your connected-store evidence and connection controls before refreshing data." : "Connect only when you are ready to approve Cresna’s stated Shopify scopes."}</p><Button onClick={() => setLocation("/app/connect")} variant="outline" className="mt-5 rounded-full text-xs font-bold">{intelligence?.store ? "Manage connection" : "Review connection"}</Button></article></section><section className="rounded-[1.35rem] border border-border bg-card p-6 text-card-foreground"><div className="flex gap-3"><ShieldCheck className="h-5 w-5 shrink-0 text-primary" /><div><p className="eyebrow text-[10px] text-muted-foreground">Data permissions</p><h2 className="mt-2 text-lg font-extrabold tracking-[-0.04em]">Cresna only uses the store data you approve.</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Store reads are explained before Shopify authorization. Cresna does not display your Shopify token, payment credentials, or saved payment details. Merchant-approved AI drafts remain reviewable and do not publish changes automatically.</p></div></div></section></div>}</WorkspaceFrame>;
}
